PROFILE="fruit_uploader"                          # must match the CLI profile you configure
DST="s3://fruit-fresca/device-$(hostname -s)"    # your bucket + per-device prefix
SRC="/data/outbox"
PING_HOST="s3.amazonaws.com"
# STORAGE_CLASS="INTELLIGENT_TIERING"             # optional; uncomment if you want it